module Ouroboros.Scheme.FromScheme (
    programFromScheme
) where

import Ouroboros.Scheme.Definition
import qualified Ouroboros.Language as AST

programFromScheme :: Scheme -> AST.Program
programFromScheme = undefined